Prompt 1: Full System (Untuk Cline / Continue)
markdown
## Context
Buat sistem informasi akuntansi untuk siklus produksi UMKM RM Pawon Selaras.

## Tech Stack
- PHP 8+ (Native, MVC sederhana)
- MySQL
- Tailwind CSS (via CDN)
- Vanilla JavaScript (no framework, no build tools)

## Project Structure (wajib diikuti)
/project-root
/app
/controllers
AuthController.php
MenuController.php
BahanController.php
ProduksiController.php
QCController.php
LaporanController.php
/models
User.php
Menu.php
Bahan.php
Resep.php
Produksi.php
QC.php
/views
/layouts
main.php
/auth
login.php
/dashboard
index.php
/menu
index.php
create.php
edit.php
/bahan
index.php
create.php
edit.php
/produksi
rencana.php
proses.php
/qc
input.php
/laporan
produksi.php
bahan.php
hpp.php
waste.php
/config
database.php
/public
/css
style.css
/js
main.js
index.php
.htaccess
/routes
web.php
/storage
database.sql

text

## Database Schema
```sql
CREATE DATABASE rm_pawon;

USE rm_pawon;

CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    password VARCHAR(255),
    role ENUM('owner', 'admin', 'kasir') DEFAULT 'admin',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE menus (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nama VARCHAR(100),
    harga_jual DECIMAL(10,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE bahan_baku (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nama VARCHAR(100),
    stok DECIMAL(10,2) DEFAULT 0,
    satuan VARCHAR(20),
    harga_per_unit DECIMAL(10,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE resep (
    id INT PRIMARY KEY AUTO_INCREMENT,
    menu_id INT,
    bahan_id INT,
    qty DECIMAL(10,2),
    FOREIGN KEY (menu_id) REFERENCES menus(id) ON DELETE CASCADE,
    FOREIGN KEY (bahan_id) REFERENCES bahan_baku(id)
);

CREATE TABLE produksi (
    id INT PRIMARY KEY AUTO_INCREMENT,
    tanggal DATE,
    menu_id INT,
    jumlah_rencana INT,
    status ENUM('perencanaan', 'produksi', 'selesai') DEFAULT 'perencanaan',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (menu_id) REFERENCES menus(id)
);

CREATE TABLE produksi_detail (
    id INT PRIMARY KEY AUTO_INCREMENT,
    produksi_id INT,
    bahan_id INT,
    qty_terpakai DECIMAL(10,2),
    FOREIGN KEY (produksi_id) REFERENCES produksi(id),
    FOREIGN KEY (bahan_id) REFERENCES bahan_baku(id)
);

CREATE TABLE qc (
    id INT PRIMARY KEY AUTO_INCREMENT,
    produksi_id INT,
    jumlah_baik INT,
    jumlah_rusak INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (produksi_id) REFERENCES produksi(id)
);

-- Insert default user (password: admin123)
INSERT INTO users (name, email, password, role) VALUES 
('Owner RM', 'owner@rmpawon.com', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 'owner'),
('Admin Dapur', 'admin@rmpawon.com', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 'admin');
Fitur yang harus dibuat (priority order)
1. Auth System
Login dengan email & password

Session management

Middleware role checking

2. Dashboard
Total stok bahan (stok < 10 tampilkan warning)

Total produksi hari ini

Total waste bulan ini

Grafik sederhana (opsional)

3. Manajemen Menu (CRUD)
List menu dengan harga

Form tambah/edit menu

BOM (resep) management per menu

Hapus menu (soft delete optional)

4. Manajemen Bahan Baku (CRUD)
List bahan dengan stok

Tambah stok (stok masuk)

Kurang stok (stok keluar untuk waste/koreksi)

Form tambah/edit bahan

5. Perencanaan Produksi
Pilih menu

Input jumlah rencana produksi

Sistem hitung kebutuhan bahan berdasarkan resep

Tampilkan tabel: bahan, stok saat ini, kebutuhan, status (cukup/kurang)

Jika semua cukup → tombol "Mulai Produksi"

Jika tidak cukup → tombol "Order Bahan" (redirect ke bahan baku)

6. Proses Produksi
Setelah klik "Mulai Produksi"

Kurangi stok bahan baku otomatis

Simpan ke tabel produksi & produksi_detail

Redirect ke form QC

7. Quality Control (QC)
Input jumlah hasil baik

Input jumlah hasil rusak (waste)

Simpan ke tabel qc

Update status produksi menjadi 'selesai'

Tampilkan HPP untuk batch ini

8. HPP Calculator
Fungsi hitungHPP(
m
e
n
u
i
d
,
menu 
i
​
 d,produksi_id)

Formula: Total biaya bahan / jumlah_baik

Tampilkan di laporan

9. Laporan (export ke HTML + print)
Laporan Produksi: filter tanggal

Laporan Bahan Baku: stok opname

Laporan HPP: per menu

Laporan Waste: per periode

Rules Coding
Gunakan prepared statement (PDO) untuk semua query

Koneksi database di config/database.php dengan singleton pattern

Setiap controller extends BaseController

Setiap model extends BaseModel

Gunakan session untuk flash message (success/error)

Semua view menggunakan Tailwind CSS + responsive

Vanilla JS untuk interaksi (fetch API untuk form submit tanpa reload)

URL pattern: index.php?route=controller/method/param

Jangan lupa .htaccess untuk hide index.php

Mulai dengan:
Buat file koneksi database

Buat BaseController dan BaseModel

Buat sistem routing sederhana

Buat layout main.php

Buat login system

Lanjutkan fitur satu per satu

Tolong tulis semua kode lengkapnya. Mulai dari config/database.php sampai ke views.

text

---

## Prompt 2: Per File (Untuk GitHub Copilot)

```markdown
@workspace 

Buatkan file [nama_file.php] untuk sistem SIA produksi RM Pawon Selaras.

Fitur: [sebutkan fitur spesifik]

Gunakan koneksi database dari $pdo yang sudah di-include.

Pattern: 
- Controller: ambil request, panggil model, load view
- Model: query dengan prepared statement
- View: HTML + Tailwind CSS

Contoh struktur:
<!-- tulis contoh yang diinginkan -->

Tulis kodenya saja tanpa penjelasan.
Prompt 3: Short & Sweet (Untuk Cline)
text
Buat fullstack web app: SIA produksi makanan (PHP + MySQL + Tailwind).

Fitur: 
- Login (owner/admin)
- CRUD menu & bahan
- Resep (BOM)
- Produksi + cek stok otomatis
- QC (baik/rusak)
- HPP otomatis
- Laporan (produksi, bahan, waste)

Database schema sudah di atas.

Ikuti struktur folder MVC: app/controllers, app/models, app/views, config/, public/, routes/.

Gunakan PDO prepared statement. Tulis kode lengkap untuk semua file.

Mulai dari config/database.php dan index.php.
Prompt 4: Debug / Fix Existing Code
markdown
@workspace 

File: [nama_file.php]

Problem: [jelaskan error atau yang tidak berfungsi]

Expected behavior: [jelaskan yang diinginkan]

Tolong fix dengan tetap mempertahankan struktur MVC dan menggunakan PDO.
Setting untuk VS Code AI Agent
Agar agent bekerja optimal:

Cline settings (di .clinerules)
text
Always use PHP with PDO prepared statements.
Follow MVC pattern: Controller handles request, Model handles database, View handles display.
Use Tailwind CSS for all styling (via CDN).
Vanilla JavaScript only, no jQuery, no build tools.
All forms must have CSRF protection (simple token).
Use session for user authentication and flash messages.
Continue extension (config.json)
json
{
  "models": [{
    "title": "Coding Agent",
    "provider": "openai",
    "model": "gpt-4o",
    "systemMessage": "Kamu adalah expert fullstack PHP. Buat kode bersih, aman, dan siap produksi untuk UMKM."
  }]
}